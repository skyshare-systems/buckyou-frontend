import { Gasoek_One, REM, Phudu } from "next/font/google";

export const gasoek_one = Gasoek_One({
  subsets: ["latin"],
  weight: ["400"],
});

export const rem = REM({
  subsets: ["latin"],
});

export const phudu = Phudu({
  subsets: ["latin"],
});
